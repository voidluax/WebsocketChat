# Optional. Without it the chat server uses an in-memory store.
# Render: create a free Postgres instance and paste its Internal Database URL.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db

# Provided automatically by Render.
# PORT=3000
# HOST=0.0.0.0
